# Nova + Navi — Public-only Repo
Deploy with Vercel using **Output Directory = public** (Framework: Other, no build command).
All site files live in /public; brain JSON at /public/data/nova_brain.json.
